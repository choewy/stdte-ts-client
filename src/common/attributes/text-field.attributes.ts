import { TextFieldProps } from '@mui/material';

export class TextFieldAttributes {
  public static Sign: TextFieldProps = {
    autoComplete: 'off',
    required: true,
    fullWidth: true,
    margin: 'normal',
  };
}
