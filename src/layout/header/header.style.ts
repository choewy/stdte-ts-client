import { SxProps } from '@mui/material';

export class HeaderStyle {
  static Wrapper(): SxProps {
    return { flexGrow: 1 };
  }

  static IconButton(): SxProps {
    return { mr: 2 };
  }

  static Title(): SxProps {
    return { flexGrow: 1 };
  }
}
